package JunitTests;


import static org.junit.Assert.*;
import org.junit.Test;
import Model.Model.BoardClasses.Square;

/**
 * Created by Grindlemire on 2/5/14.
 */
public class SquareTest {
    Square testSquare = new Square();



    @Test
    public void tester()
    {
        assertNotNull(testSquare.getPosition());
        assertFalse(testSquare.isOccupied());
        assertEquals("none", testSquare.getTeamOnSquare());
        assertNull(testSquare.getPieceOnSquare());
    }

}
